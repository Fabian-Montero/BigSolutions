@{
    ViewData["Title"] = "Mis Órdenes";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div class="container-fluid" style="margin-top: 50px;">
    <div class="text-center mb-4">
        <h1 style="padding: 15px">Mis Órdenes</h1>
        <p style="padding: 15px">Aquí puedes ver todas las órdenes que has generado.</p>
    </div>
    <div class="card" style="margin-bottom: 20px">
        <div class="card-header d-flex justify-content-between align-items-center" style="padding: 5px">
            <h2 style="padding: 15px" class="mb-0">Información del cliente</h2>
            <a href="@(Url.Action("ConsultarClientes", "Usuario"))" class="botones-secundarios">Volver</a>
        </div>
        <div style="padding: 5px">
            <p style ="padding: 15px"><strong>Nombre:</strong> Fabián</p>
        </div>
    </div>
    <div class="card">
        <div style="padding: 20px" class="card-header d-flex justify-content-between align-items-center">
            <h3 class="mb-0">Lista de órdenes</h3>
        </div>
        <div class="card-body" style="padding-left: 15px; padding-right: 15px;">
            <table id="tabla-Ordenes" class="table table-hover">
                <thead>
                    <tr>
                        <th style="text-align:left">Número de Orden</th>
                        <th style="text-align:left">Detalles de la Orden</th>
                    </tr>
                </thead>
                <tbody>
                    @* @foreach (var orden in Model)
                    { *@
                    <tr>
                        <td style="text-align:left">11111</td>
                        <td style="text-align:left">
                            <a href="@(Url.Action("ConsultarOrdenClienteAdmin", "Ordenes", new { id = 11111 }))" class="botones">Ver más detalles</a>
                        </td>
                    </tr>
                    <tr>
                        <td style="text-align:left">11112</td>
                        <td style="text-align:left">
                            <a href="@(Url.Action("ConsultarOrdenClienteAdmin", "Ordenes", new { id = 11112 }))" class="botones">Ver más detalles</a>
                        </td>
                    </tr>
                    <tr>
                        <td style="text-align:left">11113</td>
                        <td style="text-align:left">
                            <a href="@(Url.Action("ConsultarOrdenClienteAdmin", "Ordenes", new { id = 11113 }))" class="botones">Ver más detalles</a>
                        </td>
                    </tr>
                    @* } *@
                </tbody>
            </table>
        </div>
    </div>
</div>

<script src="~/lib/jquery/dist/jquery.min.js"></script>

<script>
    $(document).ready(function () {
        var table = $('#tabla-Ordenes').DataTable({
            language: {
                url: '//cdn.datatables.net/plug-ins/2.0.2/i18n/es-ES.json'
            }
        });
    });
</script>
